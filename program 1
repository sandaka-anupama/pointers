/*Sum of n numbers using pointers*/
#include<stdio.h>
int main()
{
	int *a,sum=0;
	int n,i;
	scanf("%d",&n);
	a=(int*)malloc(n*sizeof(int));
	for(i=0;i<n;i++)
	scanf("%d",a+i);
	for(i=0;i<n;i++){
	
	sum=sum+*(a+i);}
	printf("%d",sum);
	}
